Pointers:

A pointer variable holds the address of a memory location.

They are extremely powerful as they can make things easier and help increase efficiency.

Pointers can dynamically allocate memory.

It is the most distinct and exciting feature of C++ language which adds flexibility to C++.

